//
//  NYCRating.swift
//  Yelp
//
//  Created by Wenn Huang on 4/8/17.
//  Copyright © 2017 Timothy Lee. All rights reserved.
//

import UIKit
import BDBOAuth1Manager
import AFNetworking


class NYCRating: NSObject {
    /*var business : Business
    var zipCode: String
    var name : String
    
    required init?(coder aDecoder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }
    
    init(){
		let baseURL = URL(string: "http://data.cityofnewyork.us/resource/xx67-kt59.json")
		zipCode = Business.zipCode!
		name = Business.name!
		
		let request = URLRequest(url: baseURL!)
		
    }*/
    /*static let sharedInstance = NYCRating(baseURL: URL(string: "http://data.cityofnewyork.us/resource/xx67-kt59.json"), consumerKey: nil, consumerSecret: nil)

        let url = URL(string: "http://data.cityofnewyork.us/resource/xx67-kt59.json")
        let request = URLRequest(url: url!)
        let session = URLSession(configuration: URLSessionConfiguration.default,delegate:nil,
                                 delegateQueue:OperationQueue.main)
        
        let task : URLSessionDataTask = session.dataTask(with: request as URLRequest,completionHandler: { (data, response, error) in
            if let data = data {
                if let responseDictionary = try! JSONSerialization.jsonObject(with: data, options:[]) as? NSDictionary {
                    
                    let responseFieldDictionary = responseDictionary["response"] as! NSDictionary
                    
                }
            }
            
            
        });
        task.resume()*/
    
    
   
    
    
    
    
    
}
